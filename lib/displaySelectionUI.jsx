var displaySelectionUI = function(validFiles){  var userInputFolder = Folder.selectDialog("Select a folder of documents to process");  var userOutputFolder = Folder.selectDialog("Destination folder to save processed files");  validFiles.push("All files");  //form a string with quotes: "item 1","item 2"  var validFilesString = '"'+validFiles.join('","')+'"';  var dlgResource = 'dialog { text: "Choose a Wave to process", \  fileDropDown : DropDownList { properties: { \             items: ['+validFilesString+'] } }, \  btnOK : Button { text: "ok" }, \  btnCancel : Button { text: "cancel" } \  }';  var dlg = new Window(dlgResource);  dlg.center();  dlg.fileDropDown.items[0].selected = true;  if ( 1 == dlg.show() ) {    var file = dlg.fileDropDown.selection.text;    return {      "userInputFolder":userInputFolder,      "userOutputFolder":userOutputFolder,      "file": file    };  }}